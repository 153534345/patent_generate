标题title
基于哈希加密的声纹认证隐私保护方法
摘要abst
基于哈希加密的声纹认证隐私保护方法，对提取的声纹特征向量进行处理，生成二进制哈希声纹特征向量，通过随机投影然后带量化将得到的二进制哈希特征向量转换为位序列，生成哈希串特征向量q1，并将其保存到服务端。提取待验证语音音频数据的声纹特征向量，对声纹特征向量进行处理，生成二进制哈希声纹特征向量，通过随机投影然后带量化将得到的二进制哈希特征向量转换为位序列，生成哈希串特征向量q2；由哈希串特征向量q2与服务端哈希串特征向量q1进行匹配，计算q2于q1的汉明距离，得到决策结果，如完全匹配则认证，若否则拒绝。解决了声纹特征不被他人利用、泄露或篡改，保护使用者隐私的难题。
权利要求书clms
1.基于哈希加密的声纹认证隐私保护方法，包括注册阶断和认证阶段，其特征在于：所述注册阶断包括如下步骤A1，提取注册说话人的音频数据，得到音频原始文件，对音频原始文件通过声纹特征提取模型提取声纹特征向量x；A2，随机生成正交矩阵A，且A中的元素rdk服从高斯分布的独立随机变量，即：rdk～N；其中，A∈RM×L是一个随机正交矩阵；元素rdk取自具有零均值和单位方差的正态分布；N为正态分布符号N为以0为均数，1为标准差的标准正态分布；n为正交矩阵A的阶数；d为维度，等于L；k为信号长度，等于M；R为实数集符号，M为信号长度，L为信号维度数量；A3，对声纹特征向量x进行处理，生成二进制哈希声纹特征向量q，通过随机投影然后带量化将得到的二进制哈希特征向量q转换为位序列，生成哈希串特征向量q1；A4，将步骤S1和步骤S2中的音频原始文件及声纹特征向量x保存至用户端；将步骤A3中得到的哈希串特征向量q1保存到服务端；所述认证阶断包括如下步骤：B1，提取待验证语音音频数据，得到待验证音频原始文件，对待验证音频原始文件通过声纹特征提取模型提取声纹特征向量x’B2，随机生成正交矩阵A，且A中的元素rdk服从高斯分布的独立随机变量，即：rdk～N；其中，元素rdk取自具有零均值和单位方差的正态分布；N为正态分布符号；N为以0为均数，1为标准差的标准正态分布；n为正交矩阵A的阶数；d为维度，等于L；k为信号长度，等于M；R为实数集符号，M为信号长度；L为信号维度数量；B3，对声纹特征向量x'进行处理，生成二进制哈希声纹特征向量q'，通过随机投影然后带量化将二进制哈希特征向量q'转换为位序列，生成哈希串特征向量q2；B4，由哈希串特征向量q2与服务端哈希串特征向量q1进行匹配，计算q2于q1的汉明距离，得到决策结果，如完全匹配则认证，若不能完全匹配，则拒绝认证；B5，若步骤B4决策结果通过，则将步骤B1和步骤B2中的待验证音频原始文件及声纹特征向量x’保存至用户端；将步骤B3中得到的哈希串特征向量q2保存到服务端。2.根据权利要求1所述的基于哈希加密的声纹认证隐私保护方法，其特征在于：步骤A3对声纹特征向量x进行处理包括如下步骤：计算特征向量q＝Q)，生成二进制哈希q，式中Q为由Q＝给出的量化函数，y为随机变量，仅用了标识Q的计算方式；A为正交矩阵A；w为嵌入参数,w∈RM是从中均匀抽取的随机数组成的向量；Δ为精度参数；Δ-1为精度参数的倒数，-1表示倒数。w∈RM是由从均匀抽取的随机数组成的向量；R为实数集符号，M为信号长度；步骤B3对声纹特征向量x'进行处理包括如下步骤：计算特征向量q'＝Q)，生成的二进制哈希q'；式中Q为由Q＝给出的量化函数，y为随机变量，仅用了标识Q的计算方式。A为正交矩阵A；w为嵌入参数,w∈RM是从中均匀抽取的随机数组成的向量；Δ为精度参数；Δ-1为精度参数的倒数，-1表示倒数；R为实数集符号，M为信号长度。3.根据权利要求1所述的基于哈希加密的声纹认证隐私保护方法，其特征在于：步骤B4由哈希串特征向量q2与服务端哈希串特征向量q1进行匹配检索过程中通过将声纹检索过程建模为矩阵乘法并利用矩阵运算并行化技术加速检索。4.根据权利要求3所述的基于哈希加密的声纹认证隐私保护方法，其特征在于：通过将声纹检索过程建模为矩阵乘法并利用矩阵运算并行化技术加速检索包括如下步骤：考虑到底库说话人相互之间的无关性和每条查询之间的无关性，对于一个批次的查询，C1，将其每条录音通过声纹编码器编码并归一化为查询声纹；C2，将该批次的查询划分为多个小批次查询{1,2,……j……J}，每个小批次查询构成一个M×E的小批次查询矩阵Qj，其中j是批次序号；J是批次总数；M是该小批次的录音数目；E是声纹编码维度，保证M≦E且M尽量接近E；C3，对于底库数据，将底库中的声纹编码划分为多个小批次底库{1,2,…k…K},每个小批次底库构成一个N×E的小批次底库矩阵Bk，其中k是底库序号；K是底库总量；N是该小批次的底库说话人数目；E是底库数据维度数量；保证N≦E且N尽量接近E；C4，基于Cannon算法利用多处理器并行技术快速求解矩阵乘法Rjk＝QjBkT，得到小批次打分矩阵Rjk；式中j是批次序号；Qj查询矩阵；k是底库序号；Bk是底库矩阵；T是转置符号；C5，对所有底库批次合并Rjk得到Rj，再对所有查询批次合并Rj得到R即为该批次的打分矩阵R；对于每个查询u，打分矩阵R中对应行的最大响应所对应的底库说话人v*即为检索结果：v*＝argmaxv，式中argmax是计算符号，v标识说话人；Ruv是打分结果。5.根据权利要求1所述的基于哈希加密的声纹认证隐私保护方法，其特征在于：将步骤A4中得到的哈希串特征向量q1保存到服务端步骤中，采用加权异构信息加密存储的方法，将哈希串特征向量q1保存到服务端。6.根据权利要求1所述的基于哈希加密的声纹认证隐私保护方法，其特征在于：将步骤B5中得到的哈希串特征向量q2保存到服务端步骤中，采用加权异构信息加密存储的方法，将哈希串特征向量q2保存到服务端。7.根据权利要求5或6所述的基于哈希加密的声纹认证隐私保护方法，其特征在于加权异构信息加密存储的方法包括如下步骤：D1，先利用混沌序列获取加权异构信息的初始密钥，将加密后的信息输出，设置每一名用户的属性，得到用户的权限控制结构；D2，令为A'sg互联网系统的属性集，满足A'sg＝{A'sg1,A'sg2,……A'sgn}的条件，式中n为属性集A'sg中加权异构信息的数量；D3，将G上的双线映射定义为：式中P是用户个数；N是映射常数；G和GT均为乘法循环群；将g定义为乘法循环群的生成元；D4，将M个乘法循环群元素{S1，S2，……SM}∈G与A'sg属性集进行并联，在并联集合中随机选取两个指数{a,b}，计算加权异构信息的初始密钥，表示为：式中pG'as为加权异构信息的初始密钥；M是乘法循环群元素数量；D5，假设c”xv为待加密的异构信息ID，k'sgh为随机选取的对称密钥，η'为加权异构信息在加密存储过程中的时间戳，Kop为用户的权限类型，则加密后的异构信息Z'sf为：其中，dfkk表示用户访问互联网系统的结构；为tensor product.张量乘法；D6，基于加密处理后的加权异构信息，定义用户权限列表中的每一项用户属性，即：式中/＞为可以实现文件共享的用户权限列表；k'sg为互联网文件的有效状态；y'poi为互联网系统中用户的公钥；β'ouhg为已经删除权限的用户列表；j'klll为用户访问互联网系统的结构US属性集。
说明书desc
技术领域本发明涉及声纹认证领域，具体的说是基于哈希加密的声纹认证隐私保护方法。背景技术声纹认证技术是利用声纹识别进行身份认证的技术。声纹识别是根据语音中所蕴含的说话人的个性特征去识别该段语音所含说话人身份的过程。声纹认证的原理如下：首先，在注册阶断，提取语音数据，对语音数据进行提取，提取出声纹特征，将声纹特征存入数据库，从而生成声纹特征模型库；然后，在认证阶段，提取待识别语音数据，对待识别语音数据进行提取，提取出声纹特征，通过检索模型在与声纹特征模型库中的已存的声纹特征进行匹配，给出识别结果，若能匹配成功则认证通过，若不能匹配成功则不予认证通过。声纹认证已在电话银行、电子购物、医疗服务等活动中得到广泛应用。与其他生物特征识别相比，尽管声纹识别具有不会遗失和忘记、不需要记忆、使用方便等特点。但是，在声纹特征采集过程、传输、存储过程中存在泄漏、截取、篡改的风险，在认证阶段，若出现恶意输入录制好的有关语音数据，将会导致认证成功，严重的会导致无法挽回的损失。如何有效地保护声纹特征不被他人利用、泄露或篡改，保护使用者的隐私，已成为困扰声纹认证技术难题。发明内容为了解决上述技术问题，本发明提出一种基于哈希加密的声纹认证隐私保护方法，包括注册阶断和认证阶段，其中，所述注册阶断包括如下步骤A1，提取注册说话人的音频数据，得到音频原始文件，对音频原始文件通过声纹特征提取模型提取声纹特征向量x；A2，随机生成正交矩阵A，且A中的元素rdk服从高斯分布的独立随机变量，即：rdk～N；其中，A∈RM×L是一个随机正交矩阵；R为实数集符号，M为信号长度；L为信号维度数量；元素rdk取自具有零均值和单位方差的正态分布；N为正态分布符号；N为以0为均数，1为标准差的标准正态分布；n为正交矩阵A的阶数；d为维度，等于L；k为信号长度，等于M；A3，对声纹特征向量x进行处理，生成二进制哈希声纹特征向量q，通过随机投影然后带量化将得到的二进制哈希特征向量q转换为位序列，生成哈希串特征向量q1；A4，将步骤S1和步骤S2中的音频原始文件及声纹特征向量x保存至用户端；将步骤A3中得到的哈希串特征向量q1保存到服务端；所述认证阶断包括如下步骤：B1，提取待验证语音音频数据，得到待验证音频原始文件，对待验证音频原始文件通过声纹特征提取模型提取声纹特征向量x’B2，随机生成正交矩阵A，且A中的元素rdk服从高斯分布的独立随机变量，即：rdk～N；其中，N为正态分布符号N为以0为均数，1为标准差的标准正态分布n为正交矩阵A的阶数；d为维度，等于L；k为信号长度，等于M；B3，对声纹特征向量x’进行处理，生成二进制哈希声纹特征向量q'，通过随机投影然后带量化将二进制哈希特征向量q'转换为位序列，生成哈希串特征向量q2；B4，由哈希串特征向量q2与服务端哈希串特征向量q1进行匹配，计算q2于q1的汉明距离，得到决策结果，如完全匹配则认证，若不能完全匹配，则拒绝认证；B5，若步骤B4决策结果通过，则将步骤B1和步骤B2中的待验证音频原始文件及声纹特征向量x'保存至用户端；将步骤B3中得到的哈希串特征向量q2保存到服务端。进一步的，步骤A3对声纹特征向量x进行处理包括如下步骤：计算特征向量q＝Q)，生成二进制哈希q，式中Q为由Q＝给出的量化函数，y为随机变量，仅用了标识Q的计算方式。A为嵌入参数1.中的正交矩阵Aw为嵌入参数,w∈RM是从中均匀抽取的随机数组成的向量Δ为精度参数；Δ-1为精度参数的倒数，-1表示倒数。w∈RM是由从均匀抽取的随机数组成的向量；R为实数集符号，M为信号长度；进一步的，步骤B3对声纹特征向量x'进行处理包括如下步骤：计算特征向量q'＝Q)，生成的二进制哈希q'；式中Q为由Q＝给出的量化函数，y为随机变量，仅用了标识Q的计算方式。A为嵌入参数1.中的正交矩阵Aw为嵌入参数,w∈RM是从中均匀抽取的随机数组成的向量Δ为精度参数；Δ-1为精度参数的倒数，-1表示倒数。w∈RM是由从均匀抽取的随机数组成的向量；R为实数集符号，M为信号长度；进一步的，步骤B4由哈希串特征向量q2与服务端哈希串特征向量q1进行匹配检索过程中通过将声纹检索过程建模为矩阵乘法并利用矩阵运算并行化技术加速检索。进一步的，通过将声纹检索过程建模为矩阵乘法并利用矩阵运算并行化技术加速检索包括如下步骤：考虑到底库说话人相互之间的无关性和每条查询之间的无关性，对于一个批次的查询，C1，将其每条录音通过声纹编码器编码并归一化为查询声纹；C2，将该批次的查询划分为多个小批次查询{1,2,……j……J}，每个小批次查询构成一个M×E的小批次查询矩阵Qj，其中j是批次序号；J是批次总数；M是该小批次的录音数目；E是声纹编码维度，保证M≦E且M尽量接近E；C3，对于底库数据，将底库中的声纹编码划分为多个小批次底库{1,2,…k…K}；k是底库序号；K是底库总量；N是该小批次的底库说话人数目；E是底库数据维度数量；保证N≦E且N尽量接近E；C4，基于Cannon算法利用多处理器并行技术快速求解矩阵乘法Rjk＝QjBkT；得到小批次打分矩阵Rjk；式中j是批次序号；Qj查询矩阵；k是底库序号；Bk是底库矩阵；T是转置符号；C5，对所有底库批次合并Rjk得到Rj，再对所有查询批次合并Rj得到R即为该批次的打分矩阵R；对于每个查询u，打分矩阵R中对应行的最大响应所对应的底库说话人v*即为检索结果：v*＝argmaxv，式中argmax是计算符号，v标识说话人；Ruv是打分结果。进一步的，将步骤A4中得到的哈希串特征向量q1保存到服务端步骤中，采用加权异构信息加密存储的方法，将哈希串特征向量q1保存到服务端。进一步的，将步骤B5中得到的哈希串特征向量q2保存到服务端步骤中，采用加权异构信息加密存储的方法，将哈希串特征向量q2保存到服务端。进一步的，加权异构信息加密存储的方法包括如下步骤：D1，先利用混沌序列获取加权异构信息的初始密钥，将加密后的信息输出，设置每一名用户的属性，得到用户的权限控制结构；D2，令为A'sg互联网系统的属性集，满足A'sg＝{A'sg1,A'sg2,……A'sgn}的条件，式中n为属性集A'sg中加权异构信息的数量；D3，将G上的双线映射定义为：P是用户个数；N是映射常数；G和GT均为乘法循环群；将g定义为乘法循环群的生成元；D4，将M个乘法循环群元素{S1，S2，……SM}∈G与A'sg属性集进行并联，在并联集合中随机选取两个指数{a,b}，计算加权异构信息的初始密钥，表示为：pG'as为加权异构信息的初始密钥；M是乘法循环群元素数量；D5，假设c”xv为待加密的异构信息ID，k'sgh为随机选取的对称密钥，η'为加权异构信息在加密存储过程中的时间戳，Kop为用户的权限类型，则加密后的异构信息Z'sf为：其中，dfkk表示用户访问互联网系统的结构；为tensor product.张量乘法；D6，基于加密处理后的加权异构信息，定义用户权限列表中的每一项用户属性，即：式中为可以实现文件共享的用户权限列表；k'sg为互联网文件的有效状态；y'poi为互联网系统中用户的公钥；β'ouhg为已经删除权限的用户列表；j'klll为用户访问互联网系统的结构US属性集。有益效果：本发明方法将身份验证问题映射到类似于密码匹配的过程。在注册时可以保证声纹特征数据的安全性。同时也能保证用户的身份验证效率和语音信息的安全性。本发明通过将身份验证问题转化为密码匹配问题，有效消除了隐私问题。超向量和哈希的计算是在用户的客户端设备上执行的。服务器仅从注册数据和身份验证过程中获取已通过SHA-256等单向函数加密的哈希值。这种方式的说话人身份验证与基于文本的密码匹配一样安全。本方法采用基于安全二进制嵌入原理的哈希算法，将音频转换为哈希值，以允许哈希值之间存在小程度不匹配的方式相互比较。同时，算法提供信息论保证，即如果声纹向量之间的距离据够远，则向量哈希之间的距离不会提供有关向量之间真实关系的信息。此外，如果散列函数的密钥保持私有，则它们不能被反转以从它们的散列中恢复超向量。本方法通过随机投影然后将向量转换为位序列，生成的哈希向量具有以下属性：如果两个向量之间的欧氏距离低于阈值，则它们哈希之间的汉明距离与欧氏距离成正比；在阈值以上，汉明距离不提供有关两个向量之间真实距离的信息。从而有效保障了判定的准确性，避免了误判。本发明通过给定一个L维向量x∈RL，x的M位SBE定义为q＝Q)；其中Δ是精度参数，A∈RM×L是一个随机矩阵，其元素取自具有零均值和单位方差的正态分布，w∈RM是由从均匀抽取的随机数组成的向量；Q是由Q＝给出的量化函数。通过上式生成的二进制哈希q具有以下属性：给定两个具有欧几里得距离d＝||x-x'||的向量x和x'，第i个位的概率,qi和qi分别是哈希值；q和q是否相同只取决于向量之间的距离d而不是x和x'本身。q和q之间的归一化汉明距离dH,q)的概率最大为其中t是控制因子。如果d小于阈值，汉明距离E的期望为其于d保持线性；如果x和x'之间的距离大于该阈值，则dH,q)的边界为0.5-4π-2exp2)，即迅速收敛到0.5并且没有任何关于x和x’之间真实距离的信息。由此，如果嵌入参数A和w对于可能试图从其哈希向量中推断数据的潜在对手来说仍然未知，则嵌入提供了信息论安全性。任何试图从其嵌入q中恢复信号x或仅使用它们的嵌入来推断两个信号之间的关系的任何算法都将失败。本发明方法能够比较特征接近的声纹数据，而无需实际保存数据。构造了一种相对安全的分类判别器的保护方案。即保证了不可逆性、可更新性；同时对恶意输入的数据也有一定的针对性，使其的判别结果不会随意通过阈值。本发明方法通过将声纹检索过程建模为矩阵乘法并利用矩阵运算并行化技术加速检索过程。极大地减少了平均计算时间，提高了检索和匹配效率。本发明采用加权异构信息加密存储的方法，将哈希串特征向量q2保存到服务端，使用混沌序列方法，将加权异构信息的公钥和主密钥作为初始密钥，设置用户访问权限的限制条件，并通过用户权限控制结构设置用户的访问权限。极大地提升了加权异构信息的加密存储性能。附图说明图1为本发明方法示意图。具体实施方式实施例1：如图1所示，一种基于哈希加密的声纹认证隐私保护方法，包括注册阶断和认证阶段，其中，所述注册阶断包括如下步骤A1，提取注册说话人的音频数据，得到音频原始文件，对音频原始文件通过声纹特征提取模型提取声纹特征向量x；A2，随机生成正交矩阵A，且A中的元素rdk服从高斯分布的独立随机变量，即：rdk～N；其中，A∈RM×L是一个随机正交矩阵；R为实数集符号，M为信号长度；L为信号维度数量；元素rdk取自具有零均值和单位方差的正态分布；N为正态分布符号N为以0为均数，1为标准差的标准正态分布；n为正交矩阵A的阶数；d为维度，等于L；k为信号长度，等于M；A3，对声纹特征向量x进行处理，生成二进制哈希声纹特征向量q，通过随机投影然后带量化将得到的二进制哈希特征向量q转换为位序列，生成哈希串特征向量q1；本实施例采用基于安全二进制嵌入原理的哈希算法，将音频转换为哈希值，以允许哈希值之间存在小程度不匹配的方式相互比较。同时，算法提供信息论保证，即如果声纹向量之间的距离据够远，则向量哈希之间的距离不会提供有关向量之间真实关系的信息。此外，如果散列函数的密钥保持私有，则它们不能被反转以从它们的散列中恢复超向量。本实施例通过随机投影然后将向量转换为位序列，生成的哈希向量具有以下属性：如果两个向量之间的欧氏距离低于阈值，则它们哈希之间的汉明距离与欧氏距离成正比；在阈值以上，汉明距离不提供有关两个向量之间真实距离的信息。从而有效保障了判定的准确性，避免了误判。A4，将步骤S1和步骤S2中的音频原始文件及声纹特征向量x保存至用户端；将步骤A3中得到的哈希串特征向量q1保存到服务端；所述认证阶断包括如下步骤：在客户端设备上，B1，提取待验证语音音频数据，得到待验证音频原始文件，对待验证音频原始文件通过声纹特征提取模型提取声纹特征向量x’B2，随机生成正交矩阵A，且A中的元素rdk服从高斯分布的独立随机变量，即：rdk～N；其中，N为正态分布符号；N为以0为均数，1为标准差的标准正态分布；n为正交矩阵A的阶数；d为维度，等于L；k为信号长度，等于M；R为实数集符号，M为信号长度；L为信号维度数量；B3，对声纹特征向量x'进行处理，生成二进制哈希声纹特征向量q'，通过随机投影然后带量化将二进制哈希特征向量q'转换为位序列，生成哈希串特征向量q2；B4，由哈希串特征向量q2与服务端哈希串特征向量q1进行匹配，计算q2于q1的汉明距离，得到决策结果，如完全匹配则认证，若不能完全匹配，则拒绝认证；B5，若步骤B4决策结果通过，则将步骤B1和步骤B2中的待验证音频原始文件及声纹特征向量x'保存至用户端；将步骤B3中得到的哈希串特征向量q2保存到服务端。本实施例将身份验证问题映射到类似于密码匹配的过程。在注册时可以保证声纹特征数据的安全性。同时也能保证用户的身份验证效率和语音信息的安全性。本实施例通过将身份验证问题转化为密码匹配问题，有效消除了隐私问题。本实施例超向量和哈希的计算是在用户的客户端设备上执行的。服务器仅从注册数据和身份验证过程中获取已通过SHA-256等单向函数加密的哈希值。这种方式的说话人身份验证与基于文本的密码匹配一样安全。进一步的，步骤A3对声纹特征向量x进行处理包括如下步骤：计算特征向量q＝Q)，生成二进制哈希q，式中Q为由Q＝给出的量化函数，y为A为嵌入参数w为嵌入参数Δ为精度参数；Δ-1为精度参数的倒数，-1表示倒数。w∈RM是由从均匀抽取的随机数组成的向量；R为实数集符号，M为信号长度；进一步的，步骤B3对声纹特征向量x’进行处理包括如下步骤：计算特征向量q'＝Q)，生成的二进制哈希q'；式中Q为由Q＝给出的量化函数，y为A为嵌入参数w为嵌入参数Δ为精度参数；Δ-1为精度参数的倒数，-1表示倒数；w∈RM是由从均匀抽取的随机数组成的向量；R为实数集符号，M为信号长度；本实施例通过给定一个L维向量x∈RL，x的M位SBE定义为q＝Q)；其中Δ是精度参数，A∈RM×L是一个随机矩阵，其元素取自具有零均值和单位方差的正态分布，w∈RM是由从均匀抽取的随机数组成的向量；Q是由Q＝给出的量化函数。通过上式生成的二进制哈希q具有以下属性：给定两个具有欧几里得距离d＝||x-x'||的向量x和x'，第i个位的概率,qi和qi分别是哈希值；q和q是否相同只取决于向量之间的距离d而不是x和x'本身。q和q之间的归一化汉明距离dH,q)的概率最大为其中t是控制因子。如果d小于阈值，汉明距离E的期望为其于d保持线性；如果x和x'之间的距离大于该阈值，则dH,q)的边界为0.5-4π-2exp2)，即迅速收敛到0.5并且没有任何关于x和x’之间真实距离的信息。由此，如果嵌入参数A和w对于可能试图从其哈希向量中推断数据的潜在对手来说仍然未知，则嵌入提供了信息论安全性。任何试图从其嵌入q中恢复信号x或仅使用它们的嵌入来推断两个信号之间的关系的任何算法都将失败。本发明方法能够比较特征接近的声纹数据，而无需实际保存数据。构造了一种相对安全的分类判别器的保护方案。即保证了不可逆性、可更新性；同时对恶意输入的数据也有一定的针对性，使其的判别结果不会随意通过阈值。实施例2：一种基于哈希加密的声纹认证隐私保护方法，包括注册阶断和认证阶段；所述注册阶断包括如下步骤A1，提取注册说话人的音频数据，得到音频原始文件，对音频原始文件通过声纹特征提取模型提取声纹特征向量x；A2，随机生成正交矩阵A，且A中的元素rdk服从高斯分布的独立随机变量，即：rdk～N；其中，A∈RM×L是一个随机正交矩阵；R为实数集符号，M为信号长度；L为信号维度数量；元素rdk取自具有零均值和单位方差的正态分布；N为正态分布符号；N为以0为均数，1为标准差的标准正态分布；n为正交矩阵A的阶数；d为维度，等于L；k为信号长度，等于M；A3，对声纹特征向量x进行处理，生成二进制哈希声纹特征向量q，通过随机投影然后带量化将得到的二进制哈希特征向量q转换为位序列，生成哈希串特征向量q1；进一步的，步骤A3对声纹特征向量x进行处理包括如下步骤：计算特征向量q＝Q)，生成二进制哈希q，式中Q为由Q＝给出的量化函数，y为A为嵌入参数w为嵌入参数Δ为精度参数；Δ-1为精度参数的倒数，-1表示倒数。w∈RM是由从均匀抽取的随机数组成的向量；R为实数集符号，M为信号长度；A4，将步骤S1和步骤S2中的音频原始文件及声纹特征向量x保存至用户端；将步骤A3中得到的哈希串特征向量q1保存到服务端；进一步的，将步骤A4中得到的哈希串特征向量q1保存到服务端步骤中，采用加权异构信息加密存储的方法，将哈希串特征向量q1保存到服务端。进一步的，加权异构信息加密存储包括如下步骤：D1，先利用混沌序列获取加权异构信息的初始密钥，将加密后的信息输出，设置每一名用户的属性，得到用户的权限控制结构；D2，令为A'sg互联网系统的属性集，满足A'sg＝{A'sg1,A'sg2,……A'sgn}的条件，式中n为属性集A'sg中加权异构信息的数量；D3，将G上的双线映射定义为：,式中P是用户个数；N是映射常数；G和GT为乘法循环群；将g定义为乘法循环群的生成元；D4，将M个乘法循环群元素{S1，S2，……SM}∈G与A'sg属性集进行并联，在并联集合中随机选取两个指数{a,b}，计算加权异构信息的初始密钥，表示为：式中pG'as为加权异构信息的初始密钥；M是乘法循环群元素数量；D5，假设c”xv为待加密的异构信息ID，k'sgh为随机选取的对称密钥，η'为加权异构信息在加密存储过程中的时间戳，Kop为用户的权限类型，则加密后的异构信息Z'sf为：其中，dfkk表示用户访问互联网系统的结构；为tensor product.张量乘法；D6，基于加密处理后的加权异构信息，定义用户权限列表中的每一项用户属性，即：式中为可以实现文件共享的用户权限列表；k'sg为互联网文件的有效状态；y'poi为互联网系统中用户的公钥；β'ouhg为已经删除权限的用户列表；j'klll为用户访问互联网系统的结构US属性集。所述认证阶断包括如下步骤：B1，提取待验证语音音频数据，得到待验证音频原始文件，对待验证音频原始文件通过声纹特征提取模型提取声纹特征向量x’B2，随机生成正交矩阵A，且A中的元素rdk服从高斯分布的独立随机变量，即：rdk～N；其中，N为正态分布符号；N为以0为均数，1为标准差的标准正态分布；n为正交矩阵A的阶数；d为维度，等于L；k为信号长度，等于M；B3，对声纹特征向量x'进行处理，生成二进制哈希声纹特征向量q'，通过随机投影然后带量化将二进制哈希特征向量q'转换为位序列，生成哈希串特征向量q2；进一步的，步骤B3对声纹特征向量x'进行处理包括如下步骤：计算特征向量q'＝Q)，生成的二进制哈希q'；式中Q为由Q＝给出的量化函数，y为A为嵌入参数；w为嵌入参数；Δ为精度参数；Δ-1为精度参数的倒数，-1表示倒数；w∈RM是由从均匀抽取的随机数组成的向量；R为实数集符号，M为信号长度；B4，由哈希串特征向量q2与服务端哈希串特征向量q1进行匹配，计算q2于q1的汉明距离，得到决策结果，如完全匹配则认证，若不能完全匹配，则拒绝认证；进一步的，步骤B4由哈希串特征向量q2与服务端哈希串特征向量q1进行匹配检索过程中通过将声纹检索过程建模为矩阵乘法并利用矩阵运算并行化技术加速检索；进一步的，通过将声纹检索过程建模为矩阵乘法并利用矩阵运算并行化技术加速检索包括如下步骤：考虑到底库说话人相互之间的无关性和每条查询之间的无关性，对于一个批次的查询，C1，将其每条录音通过声纹编码器编码并归一化为查询声纹；C2，将该批次的查询划分为多个小批次查询{1,2,……j……J}，每个小批次查询构成一个M×E的小批次查询矩阵Qj，其中j是批次序号；J是批次总数；M是该小批次的录音数目；E是声纹编码维度，保证M≦E且M尽量接近E；C3，对于底库数据，将底库中的声纹编码划分为多个小批次底库{1,2,…k…K},每个小批次底库构成一个N×E的小批次底库矩阵Bk，其中k是底库序号；K是底库总量；N是该小批次的底库说话人数目；E是底库数据维度数量；保证N≦E且N尽量接近E；C4，基于Cannon算法利用多处理器并行技术快速求解矩阵乘法Rjk＝QjBkT，得到小批次打分矩阵Rjk；式中j是批次序号；Qj查询矩阵；k是底库序号；Bk是底库矩阵；T是转置符号；C5，对所有底库批次合并Rjk得到Rj，再对所有查询批次合并Rj得到R即为该批次的打分矩阵R；对于每个查询u，打分矩阵R中对应行的最大响应所对应的底库说话人v*即为检索结果：v*＝argmaxv，式中argmax是计算符号，v标识说话人；Ruv是打分结果。本实施例通过将声纹检索过程建模为矩阵乘法并利用矩阵运算并行化技术加速检索过程，极大地减少了平均计算时间，提高了检索和匹配效率。B5，若步骤B4决策结果通过，则将步骤B1和步骤B2中的待验证音频原始文件及声纹特征向量x'保存至用户端；将步骤B3中的哈希串特征向量q2保存到服务端。进一步的，将步骤B5中得到的哈希串特征向量q2保存到服务端步骤中，采用加权异构信息加密存储的方法，将哈希串特征向量q2保存到服务端。进一步的，加权异构信息加密存储包括如下步骤：D1，先利用混沌序列获取加权异构信息的初始密钥，将加密后的信息输出，设置每一名用户的属性，得到用户的权限控制结构；D2，令为A'sg互联网系统的属性集，满足A'sg＝{A'sg1,A'sg2,……A'sgn}的条件，式中n为属性集A'sg中加权异构信息的数量；D3，将G上的双线映射定义为：式中P是用户个数；N是映射常数；G和GT均为乘法循环群；将g定义为乘法循环群的生成元；D4，将M个乘法循环群元素{S1，S2，……SM}∈G与A'sg属性集进行并联，在并联集合中随机选取两个指数{a,b}，计算加权异构信息的初始密钥，表示为：式中pG'as为加权异构信息的初始密钥；M是乘法循环群元素数量；D5，假设c”xv为待加密的异构信息ID，k'sgh为随机选取的对称密钥，η'为加权异构信息在加密存储过程中的时间戳，Kop为用户的权限类型，则加密后的异构信息Z'sf为：其中，dfkk表示用户访问互联网系统的结构；为tensor product.张量乘法；D6，基于加密处理后的加权异构信息，定义用户权限列表中的每一项用户属性，即：式中为可以实现文件共享的用户权限列表；k'sg为互联网文件的有效状态；y'poi为互联网系统中用户的公钥；β'ouhg为已经删除权限的用户列表；j'klll为用户访问互联网系统的结构US属性集。本实施例采用加权异构信息加密存储的方法，将哈希串特征向量保存到服务端，使用混沌序列方法，将加权异构信息的公钥和主密钥作为初始密钥，设置用户访问权限的限制条件，并通过用户权限控制结构设置用户的访问权限，极大地提升了加权异构信息的加密存储性能。
